
services:

  nginx:
    container_name: nginx
    build:
      context: ./nginx
      dockerfile: Dockerfile
    image: nginx:inception42
    # env_file:
    #   - .env  
    ports:
      - "8037:80"
    # volumes:
    #   - wordpress:/var/www/wordpress
    # depends_on:
    #   - wordpress
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
     - mynet 
    restart: always

  websrv:
    container_name: webserv
    build:
      context: ./webserverjdid
      dockerfile: Dockerfile
    image: webserv:inception42
    # ports:

    networks:
      - mynet
    restart: always



networks:
  mynet:
    driver: bridge